[phases.setup]
nixPkgs = ["ffmpeg"]

[phases.install]
# Playwright браузер скачивается при старте бота (_ensure_playwright_browser в main())
# а не здесь — Railway не переносит build-кэш в runtime-контейнер
cmds = ["pip install -r requirements.txt"]
